# S2.8 任务工作台 IA + 对话持久化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对话持久化（重启不丢、可回放含卡片）+ Accio Work 式 IA（左侧边栏 / 中央 hero+会话 / 右侧按需抽屉），crew-bar 退役。

**Architecture:** Stage 1 后端 — 新 `conversation-store.ts`（镜像 contents 目录模式 + temp+rename 原子写）、新 `chat-persist.ts` 编排层（主进程全权：载历史→跑 loop→成对落盘）、channels 28→31。Stage 2 渲染层 — 新 `sidebar.js`/`drawer.js`，`chat.js` 改 conversation_id 驱动，现有五 panel 原样移入抽屉。

**Tech Stack:** TypeScript + vitest（src/）；渲染层无框架纯 JS（无单测，验证靠 CDP 冒烟）；Electron。

**Spec:** `docs/superpowers/specs/2026-06-11-sprint2.8-task-workbench-design.md`（创始人已批）。

---

## 执行环境（cwd 纪律 — 持久记忆 autocrew-worktree-smoke-discipline）

```bash
git -C /Users/macmini/projects/AutoCrew worktree add /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 -b feature/sprint2.8-task-workbench
```

- 所有 build/test/commit 一律 `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && ...` 开头；commit 后核对输出分支名。
- 收尾固定序列：`cd /Users/macmini/projects/AutoCrew` → merge → 验证 → `git worktree remove` → `branch -d`。
- 下文 `$WT` = `/Users/macmini/projects/AutoCrew/.worktrees/sprint2.8`。

**红灯纪律（上一交接固化）：** 计划中代码逐字使用；若 TDD 红灯证明计划代码有缺陷，允许最小修正并在 PR 注记中报告。

---

## Task 1: conversation-store — 类型 + 建会话 + 读会话

**Files:**
- Create: `src/storage/conversation-store.ts`
- Test: `src/storage/conversation-store.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// src/storage/conversation-store.test.ts
import fs from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import {
  makeTitle,
  createConversation,
  getConversation,
} from "./conversation-store.js";

let dir: string;

beforeEach(async () => {
  dir = await fs.mkdtemp(path.join(os.tmpdir(), "autocrew-conv-test-"));
});

afterEach(async () => {
  await fs.rm(dir, { recursive: true, force: true });
});

describe("makeTitle", () => {
  it("uses the full message when short", () => {
    expect(makeTitle("帮我写一条抖音口播")).toBe("帮我写一条抖音口播");
  });

  it("truncates at 30 code points with ellipsis (CJK-safe)", () => {
    const msg = "这".repeat(31);
    const title = makeTitle(msg);
    expect(Array.from(title)).toHaveLength(31); // 30 字 + …
    expect(title.endsWith("…")).toBe(true);
  });

  it("collapses whitespace", () => {
    expect(makeTitle("  写  一条\n口播  ")).toBe("写 一条 口播");
  });
});

describe("createConversation / getConversation", () => {
  it("creates meta + empty messages, readable back", async () => {
    const meta = await createConversation("帮我写 Excel 快捷键口播", dir);
    expect(meta.id).toMatch(/^conv-\d+-[a-z0-9]+$/);
    expect(meta.title).toBe("帮我写 Excel 快捷键口播");
    expect(meta.turns).toBe(0);
    const conv = await getConversation(meta.id, dir);
    expect(conv).not.toBeNull();
    expect(conv!.meta.id).toBe(meta.id);
    expect(conv!.messages).toEqual([]);
  });

  it("returns null for unknown id", async () => {
    expect(await getConversation("conv-123-abcdef", dir)).toBeNull();
  });

  it("rejects path-traversal ids", async () => {
    expect(await getConversation("../contents", dir)).toBeNull();
    expect(await getConversation("conv-1-x/../../y", dir)).toBeNull();
  });

  it("returns null for corrupt meta.json", async () => {
    const meta = await createConversation("hello", dir);
    await fs.writeFile(
      path.join(dir, "conversations", meta.id, "meta.json"),
      "{ not json",
      "utf-8",
    );
    expect(await getConversation(meta.id, dir)).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/storage/conversation-store.test.ts`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: Write minimal implementation**

```ts
// src/storage/conversation-store.ts
/**
 * Conversation store — 对话持久化（S2.8）。
 *
 * 目录模式镜像 contents：~/.autocrew/conversations/<id>/
 *   meta.json      — ConversationMeta（列表只读此文件，廉价）
 *   messages.json  — ConversationMessage[]（回放全量）
 *
 * 与 local-store 的差异（有意为之）：写入一律 temp+rename 原子写——
 * 会话文件每轮整体重写，进程中断不能留半个 JSON。
 * id 经正则校验（renderer 可控输入，防路径穿越）。
 */
import path from "node:path";
import fs from "node:fs/promises";
import { getDataDir } from "./local-store.js";

export interface ConversationMeta {
  id: string;
  title: string;
  createdAt: string;
  updatedAt: string;
  turns: number;
}

export interface ConversationMessage {
  role: "user" | "assistant";
  content: string;
  /** 仅 assistant：本轮工具产出的卡片，只做回放渲染，不进 LLM 上下文 */
  cards?: Record<string, unknown>[];
  ts: string;
}

export interface Conversation {
  meta: ConversationMeta;
  messages: ConversationMessage[];
}

const TITLE_MAX = 30;
const ID_RE = /^conv-\d+-[a-z0-9]+$/;

/** 标题 = 首条 user 消息（空白折叠）按码点截 30 字，CJK 安全；超长加 … */
export function makeTitle(firstMessage: string): string {
  const trimmed = firstMessage.trim().replace(/\s+/g, " ");
  const chars = Array.from(trimmed);
  if (chars.length <= TITLE_MAX) return trimmed || "（空白会话）";
  return chars.slice(0, TITLE_MAX).join("") + "…";
}

async function conversationsRoot(dataDir?: string): Promise<string> {
  const root = path.join(getDataDir(dataDir), "conversations");
  await fs.mkdir(root, { recursive: true });
  return root;
}

/** temp + rename 原子写（同目录，rename 不跨设备） */
async function writeJsonAtomic(filePath: string, value: unknown): Promise<void> {
  const tmp = `${filePath}.tmp-${process.pid}-${Date.now()}`;
  await fs.writeFile(tmp, JSON.stringify(value, null, 2), "utf-8");
  await fs.rename(tmp, filePath);
}

async function readJson<T>(filePath: string): Promise<T | null> {
  try {
    return JSON.parse(await fs.readFile(filePath, "utf-8")) as T;
  } catch {
    return null;
  }
}

export async function createConversation(
  firstUserMessage: string,
  dataDir?: string,
): Promise<ConversationMeta> {
  const root = await conversationsRoot(dataDir);
  const id = `conv-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
  const now = new Date().toISOString();
  const meta: ConversationMeta = {
    id,
    title: makeTitle(firstUserMessage),
    createdAt: now,
    updatedAt: now,
    turns: 0,
  };
  const convDir = path.join(root, id);
  await fs.mkdir(convDir, { recursive: true });
  await writeJsonAtomic(path.join(convDir, "meta.json"), meta);
  await writeJsonAtomic(path.join(convDir, "messages.json"), []);
  return meta;
}

export async function getConversation(
  id: string,
  dataDir?: string,
): Promise<Conversation | null> {
  if (!ID_RE.test(id)) return null;
  const root = await conversationsRoot(dataDir);
  const convDir = path.join(root, id);
  const meta = await readJson<ConversationMeta>(path.join(convDir, "meta.json"));
  const messages = await readJson<ConversationMessage[]>(path.join(convDir, "messages.json"));
  if (!meta || typeof meta.id !== "string" || !Array.isArray(messages)) return null;
  return { meta, messages };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/storage/conversation-store.test.ts`
Expected: PASS（全绿）。

- [ ] **Step 5: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add src/storage/conversation-store.ts src/storage/conversation-store.test.ts && git commit -m "feat: conversation store — create/get with atomic writes and id validation"
```

---

## Task 2: conversation-store — appendTurn / list / delete + 容错

**Files:**
- Modify: `src/storage/conversation-store.ts`（文件末尾追加）
- Test: `src/storage/conversation-store.test.ts`（追加）

- [ ] **Step 1: Write the failing test（追加到 conversation-store.test.ts，import 处加 appendTurn, listConversations, deleteConversation）**

```ts
describe("appendTurn", () => {
  it("appends user+assistant pair, bumps turns and updatedAt", async () => {
    const meta = await createConversation("第一轮", dir);
    const updated = await appendTurn(
      meta.id,
      { content: "第一轮" },
      { content: "好的，稿子来了", cards: [{ type: "draft", data: { title: "x" } }] },
      dir,
    );
    expect(updated).not.toBeNull();
    expect(updated!.turns).toBe(1);
    const conv = await getConversation(meta.id, dir);
    expect(conv!.messages).toHaveLength(2);
    expect(conv!.messages[0]).toMatchObject({ role: "user", content: "第一轮" });
    expect(conv!.messages[1].role).toBe("assistant");
    expect(conv!.messages[1].cards).toHaveLength(1);
  });

  it("omits cards key when empty", async () => {
    const meta = await createConversation("无卡轮", dir);
    await appendTurn(meta.id, { content: "无卡轮" }, { content: "回复", cards: [] }, dir);
    const conv = await getConversation(meta.id, dir);
    expect(conv!.messages[1]).not.toHaveProperty("cards");
  });

  it("returns null for missing conversation", async () => {
    expect(await appendTurn("conv-1-gone", { content: "a" }, { content: "b" }, dir)).toBeNull();
  });
});

describe("listConversations", () => {
  it("sorts by updatedAt desc and skips corrupt entries", async () => {
    const a = await createConversation("旧会话", dir);
    await new Promise((r) => setTimeout(r, 5));
    const b = await createConversation("新会话", dir);
    // 损坏第三个
    const c = await createConversation("坏会话", dir);
    await fs.writeFile(path.join(dir, "conversations", c.id, "meta.json"), "broken", "utf-8");
    const list = await listConversations(dir);
    expect(list.map((m) => m.id)).toEqual([b.id, a.id]);
  });

  it("returns [] when nothing exists", async () => {
    expect(await listConversations(dir)).toEqual([]);
  });
});

describe("deleteConversation", () => {
  it("removes the directory and returns true", async () => {
    const meta = await createConversation("待删", dir);
    expect(await deleteConversation(meta.id, dir)).toBe(true);
    expect(await getConversation(meta.id, dir)).toBeNull();
  });

  it("returns false for unknown or invalid id", async () => {
    expect(await deleteConversation("conv-1-nothere", dir)).toBe(false);
    expect(await deleteConversation("../contents", dir)).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/storage/conversation-store.test.ts`
Expected: FAIL — appendTurn 等未导出。

- [ ] **Step 3: Write minimal implementation（追加到 conversation-store.ts）**

```ts
export async function appendTurn(
  id: string,
  user: { content: string },
  assistant: { content: string; cards?: Record<string, unknown>[] },
  dataDir?: string,
): Promise<ConversationMeta | null> {
  const existing = await getConversation(id, dataDir);
  if (!existing) return null;
  const root = await conversationsRoot(dataDir);
  const convDir = path.join(root, id);
  const now = new Date().toISOString();
  const messages = existing.messages;
  messages.push({ role: "user", content: user.content, ts: now });
  messages.push({
    role: "assistant",
    content: assistant.content,
    ...(assistant.cards && assistant.cards.length > 0 ? { cards: assistant.cards } : {}),
    ts: now,
  });
  const meta: ConversationMeta = { ...existing.meta, turns: existing.meta.turns + 1, updatedAt: now };
  await writeJsonAtomic(path.join(convDir, "messages.json"), messages);
  await writeJsonAtomic(path.join(convDir, "meta.json"), meta);
  return meta;
}

export async function listConversations(dataDir?: string): Promise<ConversationMeta[]> {
  const root = await conversationsRoot(dataDir);
  const entries = await fs.readdir(root, { withFileTypes: true });
  const metas: ConversationMeta[] = [];
  for (const e of entries) {
    if (!e.isDirectory()) continue;
    const meta = await readJson<ConversationMeta>(path.join(root, e.name, "meta.json"));
    if (!meta || typeof meta.id !== "string" || typeof meta.updatedAt !== "string") {
      console.warn(`[conversation-store] 跳过损坏会话：${e.name}`);
      continue;
    }
    metas.push(meta);
  }
  return metas.sort((a, b) => b.updatedAt.localeCompare(a.updatedAt));
}

export async function deleteConversation(id: string, dataDir?: string): Promise<boolean> {
  if (!ID_RE.test(id)) return false;
  const root = await conversationsRoot(dataDir);
  const convDir = path.join(root, id);
  try {
    await fs.access(convDir);
  } catch {
    return false;
  }
  await fs.rm(convDir, { recursive: true, force: true });
  return true;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/storage/conversation-store.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add src/storage/conversation-store.ts src/storage/conversation-store.test.ts && git commit -m "feat: conversation store — append/list/delete with corrupt-file tolerance"
```

---

## Task 3: chat-persist — 主进程全权编排层

**Files:**
- Create: `src/desktop/chat-persist.ts`
- Test: `src/desktop/chat-persist.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// src/desktop/chat-persist.test.ts
import fs from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { runPersistedChatTurn } from "./chat-persist.js";
import {
  createConversation,
  appendTurn,
  getConversation,
  listConversations,
} from "../storage/conversation-store.js";
import type { runChatTurn } from "./chat-router.js";

let dir: string;

beforeEach(async () => {
  dir = await fs.mkdtemp(path.join(os.tmpdir(), "autocrew-persist-test-"));
});

afterEach(async () => {
  await fs.rm(dir, { recursive: true, force: true });
});

const okTurn = (reply: string, cards: Record<string, unknown>[] = []) =>
  vi.fn(async () => ({ ok: true, data: { reply, cards, tokensUsed: 10 } })) as unknown as typeof runChatTurn;

describe("runPersistedChatTurn", () => {
  it("auto-creates conversation on first successful turn and persists the pair", async () => {
    const res = await runPersistedChatTurn({
      message: "帮我写口播",
      dataDir: dir,
      runTurn: okTurn("稿子来了", [{ type: "draft", data: {} }]),
    });
    expect(res.ok).toBe(true);
    const data = res.data as Record<string, unknown>;
    expect(typeof data.conversationId).toBe("string");
    const conv = await getConversation(data.conversationId as string, dir);
    expect(conv!.meta.title).toBe("帮我写口播");
    expect(conv!.messages).toHaveLength(2);
    expect(conv!.messages[1].cards).toHaveLength(1);
  });

  it("does NOT create a conversation when the turn fails", async () => {
    const failTurn = vi.fn(async () => ({ ok: false, error: "boom" })) as unknown as typeof runChatTurn;
    const res = await runPersistedChatTurn({ message: "hi", dataDir: dir, runTurn: failTurn });
    expect(res.ok).toBe(false);
    expect(await listConversations(dir)).toEqual([]);
  });

  it("does NOT create a conversation on needsSetup", async () => {
    const setupTurn = vi.fn(async () => ({ ok: false, needsSetup: true, error: "no engine" })) as unknown as typeof runChatTurn;
    const res = await runPersistedChatTurn({ message: "hi", dataDir: dir, runTurn: setupTurn });
    expect(res.needsSetup).toBe(true);
    expect(await listConversations(dir)).toEqual([]);
  });

  it("loads prior messages as text-only history, windowed to 12", async () => {
    const meta = await createConversation("长会话", dir);
    for (let i = 0; i < 10; i++) {
      await appendTurn(
        meta.id,
        { content: `问${i}` },
        { content: `答${i}`, cards: [{ type: "draft", data: {} }] },
        dir,
      );
    }
    const spy = vi.fn(async () => ({ ok: true, data: { reply: "ok", cards: [], tokensUsed: 1 } }));
    await runPersistedChatTurn({
      message: "继续",
      conversationId: meta.id,
      dataDir: dir,
      runTurn: spy as unknown as typeof runChatTurn,
    });
    const call = spy.mock.calls[0][0] as { history: Array<Record<string, unknown>> };
    expect(call.history).toHaveLength(12);
    expect(call.history[0]).toEqual({ role: "user", content: "问4" });
    expect(call.history[11]).toEqual({ role: "assistant", content: "答9" });
    expect(call.history.every((m) => !("cards" in m))).toBe(true);
  });

  it("rejects unknown conversation id without running the turn", async () => {
    const spy = vi.fn();
    const res = await runPersistedChatTurn({
      message: "hi",
      conversationId: "conv-1-gone",
      dataDir: dir,
      runTurn: spy as unknown as typeof runChatTurn,
    });
    expect(res.ok).toBe(false);
    expect(String(res.error)).toContain("会话不存在");
    expect(spy).not.toHaveBeenCalled();
  });

  it("appends to an existing conversation and echoes its id", async () => {
    const meta = await createConversation("续聊", dir);
    await appendTurn(meta.id, { content: "续聊" }, { content: "好" }, dir);
    const res = await runPersistedChatTurn({
      message: "再来",
      conversationId: meta.id,
      dataDir: dir,
      runTurn: okTurn("来了"),
    });
    expect((res.data as Record<string, unknown>).conversationId).toBe(meta.id);
    const conv = await getConversation(meta.id, dir);
    expect(conv!.messages).toHaveLength(4);
    expect(conv!.meta.turns).toBe(2);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/desktop/chat-persist.test.ts`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: Write minimal implementation**

```ts
// src/desktop/chat-persist.ts
/**
 * chat:turn 的持久化编排（S2.8 主进程全权）：
 * 载入会话历史（最近 12 条纯文本，卡片不进上下文）→ runChatTurn →
 * 成功后 user+assistant 成对原子落盘 → data.conversationId 回传。
 *
 * 失败/needsSetup 不落盘；无 conversation_id 的首轮成功后才建会话（不留空壳）。
 * runTurn 为测试注入口（镜像 ChatToolDeps 模式）。
 */
import { runChatTurn, type ChatHistoryMessage, type ChatProgressEvent } from "./chat-router.js";
import {
  createConversation,
  getConversation,
  appendTurn,
} from "../storage/conversation-store.js";

const HISTORY_WINDOW = 12;

export async function runPersistedChatTurn(params: {
  message: string;
  conversationId?: string;
  dataDir?: string;
  onEvent?: (e: ChatProgressEvent) => void;
  runTurn?: typeof runChatTurn;
}): Promise<Record<string, unknown>> {
  const run = params.runTurn ?? runChatTurn;
  const { message, conversationId, dataDir } = params;

  let history: ChatHistoryMessage[] = [];
  if (conversationId) {
    const conv = await getConversation(conversationId, dataDir);
    if (!conv) return { ok: false, error: "会话不存在或已损坏，请新建任务" };
    history = conv.messages
      .slice(-HISTORY_WINDOW)
      .map((m) => ({ role: m.role, content: m.content }));
  }

  const result = await run({
    message,
    history,
    dataDir,
    ...(params.onEvent ? { onEvent: params.onEvent } : {}),
  });
  if (!result.ok) return result;

  const data = result.data as { reply?: unknown; cards?: unknown } | undefined;
  const reply = typeof data?.reply === "string" ? data.reply : "";
  const cards = Array.isArray(data?.cards) ? (data.cards as Record<string, unknown>[]) : [];

  const convId = conversationId ?? (await createConversation(message, dataDir)).id;
  const meta = await appendTurn(convId, { content: message }, { content: reply, cards }, dataDir);
  return {
    ...result,
    data: { ...(result.data as Record<string, unknown>), conversationId: meta ? convId : null },
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/desktop/chat-persist.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add src/desktop/chat-persist.ts src/desktop/chat-persist.test.ts && git commit -m "feat: chat-persist — main-process-owned conversation persistence around chat turns"
```

---

## Task 4: IPC 28 → 31 + chat:turn 接通持久化

**Files:**
- Modify: `src/desktop/channels.ts`（通道数组 + 文档注释）
- Modify: `src/desktop/ipc.ts`（3 新 handler + chatTurnHandler 改写 + 头部注释表）
- Test: `src/desktop/ipc.test.ts`

- [ ] **Step 1: Write the failing test**

ipc.test.ts 三处修改：

1. 头部注释 `all 28 channels` → `all 31 channels`；
2. `EXPECTED` 数组末尾追加三项，计数断言改 31：

```ts
    "draft:rewrite_selection",
    "style:record_edit",
    "conversations:list",
    "conversations:get",
    "conversations:delete",
  ];

  it("has exactly 31 channels", () => {
    expect(IPC_CHANNELS).toHaveLength(31);
  });
```

3. 第 147-152 行附近 `covers exactly the execute-backed channels` 测试的排除链追加：

```ts
          ch !== "conversations:list" &&
          ch !== "conversations:get" &&
          ch !== "conversations:delete" &&
```

（描述字符串里的排除清单同步加这三个名字。）

4. 文件末尾追加新 describe（沿用 testDir 模式；`createConversation`/`appendTurn` 从 `../storage/conversation-store.js` import）：

```ts
// ── 13. conversations:* handlers ─────────────────────────────────────────────

describe("conversations handlers", () => {
  it("list returns persisted conversations newest-first", async () => {
    const handlers = buildIpcHandlers();
    const a = await createConversation("会话A", testDir);
    await appendTurn(a.id, { content: "会话A" }, { content: "好" }, testDir);
    const res = await handlers["conversations:list"]({ _dataDir: testDir });
    expect(res.ok).toBe(true);
    const convs = (res.data as { conversations: Array<{ id: string }> }).conversations;
    expect(convs[0].id).toBe(a.id);
  });

  it("get returns full messages; missing id errors", async () => {
    const handlers = buildIpcHandlers();
    const a = await createConversation("会话B", testDir);
    await appendTurn(a.id, { content: "会话B" }, { content: "回", cards: [{ type: "draft", data: {} }] }, testDir);
    const res = await handlers["conversations:get"]({ id: a.id, _dataDir: testDir });
    expect(res.ok).toBe(true);
    expect((res.data as { messages: unknown[] }).messages).toHaveLength(2);
    const miss = await handlers["conversations:get"]({ id: "conv-1-gone", _dataDir: testDir });
    expect(miss.ok).toBe(false);
    const noId = await handlers["conversations:get"]({ _dataDir: testDir });
    expect(noId.ok).toBe(false);
  });

  it("delete removes; unknown id errors; guards bad payload", async () => {
    const handlers = buildIpcHandlers();
    const a = await createConversation("会话C", testDir);
    const del = await handlers["conversations:delete"]({ id: a.id, _dataDir: testDir });
    expect(del.ok).toBe(true);
    const again = await handlers["conversations:delete"]({ id: a.id, _dataDir: testDir });
    expect(again.ok).toBe(false);
    const bad = await handlers["conversations:list"](null as unknown as Record<string, unknown>);
    expect(bad.ok).toBe(false);
  });

  it("chat:turn forwards conversation_id (deps spy sees it untouched)", async () => {
    const spy = vi.fn(async (p: Record<string, unknown>) => ({ ok: true, echo: p.conversation_id }));
    const handlers = buildIpcHandlers({ "chat:turn": spy });
    const res = await handlers["chat:turn"]({ message: "hi", conversation_id: "conv-1-abc" });
    expect(res.echo).toBe("conv-1-abc");
  });
});
```

注意：既有 chat:turn 测试（空 message 守卫、null payload、deps 覆盖、progress 转发）保持通过，不删。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npx vitest run src/desktop/ipc.test.ts`
Expected: FAIL — 31 计数 + conversations handler 不存在。

- [ ] **Step 3: Implement**

`channels.ts`：数组 `"style:record_edit",` 之后追加：

```ts
  "conversations:list",
  "conversations:get",
  "conversations:delete",
```

文档注释 `All 28 methods` → `All 31 methods`，方法清单末尾加 `conversationsList / conversationsGet / conversationsDelete`。

`ipc.ts`：

1. import 区：删 `runChatTurn, type ChatHistoryMessage`（chat-router import 整行删），加：

```ts
import { runPersistedChatTurn } from "./chat-persist.js";
import { listConversations, getConversation, deleteConversation } from "../storage/conversation-store.js";
```

2. 头部注释表 `chat:turn { message, history? }` 改为 `chat:turn { conversation_id?, message }`，表尾追加：

```
 *   conversations:list   {}
 *   conversations:get    { id }
 *   conversations:delete { id }
```

3. `chatTurnHandler` 整体替换：

```ts
async function chatTurnHandler(payload: Record<string, unknown>, ctx?: IpcHandlerContext): Promise<Record<string, unknown>> {
  if (payload === null || typeof payload !== "object" || Array.isArray(payload)) {
    return { ok: false, error: "Invalid payload: expected object" };
  }
  const message = payload.message;
  if (typeof message !== "string" || message.trim() === "") {
    return { ok: false, error: "chat:turn 需要非空 message" };
  }
  const conversationId =
    typeof payload.conversation_id === "string" && payload.conversation_id !== ""
      ? payload.conversation_id
      : undefined;
  try {
    return await runPersistedChatTurn({
      message: message.trim(),
      ...(conversationId ? { conversationId } : {}),
      dataDir: (payload._dataDir as string) || undefined,
      ...(ctx?.onProgress
        ? {
            onEvent: (e: unknown) => {
              try {
                ctx.onProgress!(e as Record<string, unknown>);
              } catch {
                /* 推送失败（窗口已关）不影响生成 */
              }
            },
          }
        : {}),
    });
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err) };
  }
}
```

4. 新 handler（styleRecordEditHandler 之后）：

```ts
// ── conversations:* — 任务历史（S2.8 对话持久化） ────────────────────────────

async function conversationsListHandler(payload: Record<string, unknown>): Promise<Record<string, unknown>> {
  if (payload === null || typeof payload !== "object" || Array.isArray(payload)) {
    return { ok: false, error: "Invalid payload: expected object" };
  }
  try {
    return { ok: true, data: { conversations: await listConversations((payload._dataDir as string) || undefined) } };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err) };
  }
}

async function conversationsGetHandler(payload: Record<string, unknown>): Promise<Record<string, unknown>> {
  if (payload === null || typeof payload !== "object" || Array.isArray(payload)) {
    return { ok: false, error: "Invalid payload: expected object" };
  }
  const id = payload.id;
  if (typeof id !== "string" || !id) return { ok: false, error: "需要 id" };
  try {
    const conv = await getConversation(id, (payload._dataDir as string) || undefined);
    if (!conv) return { ok: false, error: "会话不存在或已损坏" };
    return { ok: true, data: { meta: conv.meta, messages: conv.messages } };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err) };
  }
}

async function conversationsDeleteHandler(payload: Record<string, unknown>): Promise<Record<string, unknown>> {
  if (payload === null || typeof payload !== "object" || Array.isArray(payload)) {
    return { ok: false, error: "Invalid payload: expected object" };
  }
  const id = payload.id;
  if (typeof id !== "string" || !id) return { ok: false, error: "需要 id" };
  try {
    const removed = await deleteConversation(id, (payload._dataDir as string) || undefined);
    if (!removed) return { ok: false, error: "会话不存在" };
    return { ok: true, data: {} };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err) };
  }
}
```

5. `buildIpcHandlers` defaults 末尾追加：

```ts
    "conversations:list": conversationsListHandler,
    "conversations:get": conversationsGetHandler,
    "conversations:delete": conversationsDeleteHandler,
```

（preload.ts 与 desktop/main.ts 都对 IPC_CHANNELS 泛型遍历，零改动。）

- [ ] **Step 4: Run full test suite**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npm run check`
Expected: typecheck + lint + 全部测试通过（chat-router.test.ts 不受影响——runChatTurn 签名未动）。

- [ ] **Step 5: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add src/desktop/channels.ts src/desktop/ipc.ts src/desktop/ipc.test.ts && git commit -m "feat: conversations IPC channels (28→31) + chat:turn persistence wiring"
```

---

## Task 5: 渲染层骨架 — sidebar + drawer + index.html + CSS

**Files:**
- Modify: `desktop/renderer/index.html`（整体重排）
- Create: `desktop/renderer/sidebar.js`
- Create: `desktop/renderer/drawer.js`
- Modify: `desktop/renderer/style.css`
- Modify: `desktop/renderer/app.js`（删 switchPanel/refreshActivePanel/ws-tab/crew-chip 路由，改 boot）
- Modify: `desktop/renderer/cards.js`（switchPanel → openDrawer ×4）

渲染层无单测（既有约定），本任务验证 = Task 7 CDP 冒烟；本任务内先过 `npm run build:desktop`（含 smoke-desktop-load）。

- [ ] **Step 1: index.html 整体替换 body 结构**

```html
<body>
  <div id="app-wrap">
  <div id="layout">
    <aside id="sidebar">
      <div class="logo">AutoCrew · 编辑部</div>
      <button id="new-task" class="btn-primary sidebar-new-task">＋ 新任务</button>
      <div class="sidebar-section">
        <div class="sidebar-heading">任务历史</div>
        <div id="conversation-list"></div>
      </div>
      <div class="sidebar-section">
        <div class="sidebar-heading">数字员工</div>
        <div id="crew-list"></div>
      </div>
      <div class="sidebar-section">
        <div class="sidebar-heading">即将上线</div>
        <div class="sidebar-item disabled" title="敬请期待">定时任务</div>
        <div class="sidebar-item disabled" title="敬请期待">插件</div>
        <div class="sidebar-item disabled" title="敬请期待">消息渠道</div>
        <div class="sidebar-item disabled" title="敬请期待">团队</div>
      </div>
      <div id="sidebar-footer">
        <button class="sidebar-item" id="open-settings">设置</button>
        <div class="sidebar-user" title="账号与积分将随产品账号体系上线">本地版</div>
      </div>
    </aside>
    <section id="main-area" class="hero-mode">
      <div id="hero-block">
        <div class="hero-title">编辑部就位</div>
        <p class="hero-sub">直接说需求，比如：帮我写一条关于 Excel 快捷键的抖音口播。</p>
        <div id="hero-chips"></div>
      </div>
      <div id="chat-stream"></div>
      <div id="chat-input-row">
        <textarea id="chat-input" rows="2" placeholder="告诉团队你想做什么…"></textarea>
        <button id="chat-send" class="btn-primary">发送</button>
      </div>
    </section>
    <div id="drawer-mask" class="hidden"></div>
    <aside id="drawer" class="drawer-closed" aria-hidden="true">
      <header id="drawer-header">
        <span id="drawer-title"></span>
        <button id="drawer-close" class="btn-mini" title="关闭">✕</button>
      </header>
      <div id="drawer-body">
        <section id="panel-report" class="panel"></section>
        <section id="panel-drafts" class="panel"></section>
        <section id="panel-style" class="panel"></section>
        <section id="panel-settings" class="panel"></section>
        <section id="panel-scout" class="panel"></section>
      </div>
    </aside>
  </div>
  <div id="toast" class="toast hidden"></div>
  </div>
  <script src="dom.js"></script>
  <script src="markdown.cjs"></script>
  <script src="drawer.js"></script>
  <script src="cards.js"></script>
  <script src="selection.js"></script>
  <script src="workbench.js"></script>
  <script src="chat.js"></script>
  <script src="sidebar.js"></script>
  <script src="scout.js"></script>
  <script src="settings.js"></script>
  <script src="onboarding.js"></script>
  <script src="app.js"></script>
</body>
```

（head 不动；crew-bar 与 workspace-tabs 不再存在。）

- [ ] **Step 2: 新建 drawer.js**

```js
/**
 * 右侧工作台抽屉（S2.8）：默认两栏，面板按需滑出。
 * 同时只开一个；Esc / 遮罩 / ✕ 关闭。面板渲染复用 app.js 的 init*（点击时已定义）。
 */

const DRAWER_TITLES = {
  report: "数据分析师 · 回流报告",
  drafts: "稿件",
  style: "编剧 · 风格档案",
  settings: "设置",
  scout: "选题侦察员",
};

let activeDrawerPanel = null;

function openDrawer(name) {
  if (!DRAWER_TITLES[name]) return;
  activeDrawerPanel = name;
  document.getElementById("drawer-title").textContent = DRAWER_TITLES[name];
  document.querySelectorAll("#drawer-body .panel").forEach((p) => p.classList.remove("active"));
  document.getElementById("panel-" + name).classList.add("active");
  const drawer = document.getElementById("drawer");
  drawer.classList.remove("drawer-closed");
  drawer.setAttribute("aria-hidden", "false");
  document.getElementById("drawer-mask").classList.remove("hidden");
  refreshDrawerPanel(name);
}

function closeDrawer() {
  activeDrawerPanel = null;
  const drawer = document.getElementById("drawer");
  drawer.classList.add("drawer-closed");
  drawer.setAttribute("aria-hidden", "true");
  document.getElementById("drawer-mask").classList.add("hidden");
}

function refreshDrawerPanel(name) {
  if (name === "report") initReport();
  if (name === "drafts") initDrafts();
  if (name === "style") initStyle();
  if (name === "settings" && typeof initSettings === "function") initSettings();
  if (name === "scout" && typeof initScout === "function") initScout();
}

/** 抽屉开着才刷新（对话回合后调用，原 refreshActivePanel 语义） */
function refreshActiveDrawer() {
  if (activeDrawerPanel) refreshDrawerPanel(activeDrawerPanel);
}

function initDrawer() {
  document.getElementById("drawer-close").addEventListener("click", closeDrawer);
  document.getElementById("drawer-mask").addEventListener("click", closeDrawer);
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") closeDrawer();
  });
}
```

- [ ] **Step 3: 新建 sidebar.js**

```js
/**
 * 左侧边栏（S2.8 任务工作台）：新任务 / 任务历史 / 数字员工 / 设置。
 * 列表数据源 = conversations:list；点击回放走 chat.js 的 loadConversation。
 * activeConversationId / newTask 由 chat.js 定义（脚本顺序在前）。
 */

const SIDEBAR_CREW = [
  { role: "scout", badge: "侦", name: "选题侦察员", hint: "雷达扫描赛道热点，每日推送候选选题" },
  { role: "writer", badge: "编", name: "编剧", hint: "根据选题和风格档案撰写口播稿件" },
  { role: "review", badge: "审", name: "合规审核员", hint: "检查稿件风格合规并排版发布文案" },
  { role: "analyst", badge: "析", name: "数据分析师", hint: "分析回流数据，生成 playbook 报告" },
];

async function refreshConversationList() {
  const listEl = document.getElementById("conversation-list");
  const res = await safeInvoke(window.autocrew.conversationsList);
  listEl.innerHTML = "";
  if (!res.ok) {
    listEl.appendChild(h("p", { class: "muted sidebar-empty" }, "任务列表加载失败"));
    return;
  }
  const conversations = (res.data && res.data.conversations) || [];
  if (conversations.length === 0) {
    listEl.appendChild(h("p", { class: "muted sidebar-empty" }, "还没有任务"));
    return;
  }
  for (const conv of conversations) {
    const row = h("div", {
      class: "conv-row" + (conv.id === activeConversationId ? " conv-active" : ""),
      title: conv.title,
    });
    const titleSpan = h("span", { class: "conv-title" }, conv.title);
    titleSpan.addEventListener("click", () => loadConversation(conv.id));
    row.appendChild(titleSpan);
    const delBtn = h("button", { class: "btn-mini conv-del", title: "删除任务" }, "✕");
    delBtn.addEventListener("click", async (e) => {
      e.stopPropagation();
      if (!confirm("删除任务「" + conv.title + "」？聊天记录将一并删除。")) return;
      const r = await safeInvoke(window.autocrew.conversationsDelete, { id: conv.id });
      if (!r.ok) { showToast(r.error || "删除失败"); return; }
      if (conv.id === activeConversationId) newTask();
      else refreshConversationList();
    });
    row.appendChild(delBtn);
    listEl.appendChild(row);
  }
}

function renderCrewList() {
  const crewEl = document.getElementById("crew-list");
  crewEl.innerHTML = "";
  for (const c of SIDEBAR_CREW) {
    const row = h("div", { class: "sidebar-item crew-item", title: c.hint });
    row.appendChild(h("span", { class: "byline-badge byline-badge-" + c.role }, c.badge));
    row.appendChild(h("span", {}, c.name));
    row.addEventListener("click", () => {
      if (c.role === "scout") openDrawer("scout");
      else if (c.role === "writer") openDrawer("style");
      else if (c.role === "analyst") openDrawer("report");
      else if (c.role === "review") appendReviewerCard();
    });
    crewEl.appendChild(row);
  }
}

function initSidebar() {
  document.getElementById("new-task").addEventListener("click", newTask);
  document.getElementById("open-settings").addEventListener("click", () => openDrawer("settings"));
  renderCrewList();
  refreshConversationList();
}
```

- [ ] **Step 4: app.js 改造**

删除：`switchPanel`、`refreshActivePanel`、`.ws-tab` 监听块、`.crew-chip` 路由块（“团队栏 chip 路由”整节）。
保留：`initReport` / `initDrafts` / `renderDraftDetail` / `renderPublishActions` / `initStyle` / `loadStyleRules`（抽屉面板渲染者）。
Boot 区替换为：

```js
// ── Boot ──────────────────────────────────────────────────────────────────────

initChat();
initDrawer();
initSidebar();
bootOnboarding();
renderHeroChips();
```

文件头注释改为「工作台抽屉面板（report/drafts/style）渲染者；抽屉容器在 drawer.js」。

- [ ] **Step 5: cards.js 路由替换（4 处）**

`renderDraftCard` / `renderDraftsListCard`：`switchPanel("drafts")` → `openDrawer("drafts")`；
`renderReportCard`：`switchPanel("report")` → `openDrawer("report")`；
`renderStyleCard`：`switchPanel("style")` → `openDrawer("style")`。
确认替换后全仓 `grep -rn "switchPanel" desktop/renderer/` 仅剩 chat.js 一处（Task 6 处理）。

- [ ] **Step 6: style.css 增删**

删除整块：`#crew-bar`、`.crew-chip*`、`.crew-badge`、`#workspace`、`#workspace-tabs`、`.ws-tab*`。
`#chat-pane { ... }` 选择器更名为 `#main-area`（保留原 flex column 规则）。
`.panel`/`.panel.active` 显示规则如原在 #workspace 节内，移到下方 Drawer 节。
追加（fallback 色值即 DESIGN.md 暖纸系；若 :root 已有同义 token 用 token）：

```css
/* ── Sidebar（S2.8 任务工作台）─────────────────────────────────────────── */
#sidebar {
  width: 220px; flex-shrink: 0; display: flex; flex-direction: column;
  border-right: 1px solid var(--border, #e4ddd0); padding: 12px 10px; gap: 12px;
  overflow-y: auto;
}
.sidebar-new-task { width: 100%; }
.sidebar-heading { font-size: 12px; color: var(--muted, #8a8273); margin: 4px 0 6px; }
.sidebar-item {
  display: flex; align-items: center; gap: 8px; padding: 7px 8px;
  border-radius: 8px; cursor: pointer; font-size: 13px;
  background: none; border: none; width: 100%; text-align: left;
}
.sidebar-item:hover { background: rgba(0, 0, 0, 0.05); }
.sidebar-item.disabled { opacity: 0.45; cursor: not-allowed; }
.sidebar-item.disabled:hover { background: none; }
#sidebar-footer { margin-top: auto; }
.sidebar-user { font-size: 12px; color: var(--muted, #8a8273); padding: 6px 8px; }
.sidebar-empty { padding: 4px 8px; font-size: 12px; }
.conv-row { display: flex; align-items: center; gap: 4px; padding: 6px 8px; border-radius: 8px; cursor: pointer; }
.conv-row:hover { background: rgba(0, 0, 0, 0.05); }
.conv-row .conv-del { visibility: hidden; flex-shrink: 0; }
.conv-row:hover .conv-del { visibility: visible; }
.conv-active { background: rgba(0, 0, 0, 0.08); }
.conv-title { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 13px; }

/* ── Hero 空态 ─────────────────────────────────────────────────────────── */
#hero-block { display: none; }
#main-area.hero-mode { justify-content: center; align-items: center; }
#main-area.hero-mode #hero-block { display: block; text-align: center; max-width: 560px; margin-bottom: 18px; }
#main-area.hero-mode #chat-stream { display: none; }
#main-area.hero-mode #chat-input-row { width: min(560px, 80%); flex: none; }
.hero-title { font-size: 22px; font-weight: 600; margin-bottom: 6px; }
.hero-sub { color: var(--muted, #8a8273); margin-bottom: 14px; }
#hero-chips { display: flex; gap: 8px; justify-content: center; }
.hero-chip {
  font-size: 12px; padding: 4px 10px; border-radius: 999px;
  border: 1px solid var(--border, #e4ddd0); background: none; cursor: pointer;
}

/* ── 工作台抽屉 ─────────────────────────────────────────────────────────── */
#drawer-mask { position: fixed; inset: 0; background: rgba(0, 0, 0, 0.25); z-index: 40; }
#drawer-mask.hidden { display: none; }
#drawer {
  position: fixed; top: 0; right: 0; bottom: 0; width: min(520px, 90vw);
  background: var(--panel-bg, #faf6ee); border-left: 1px solid var(--border, #e4ddd0);
  z-index: 50; display: flex; flex-direction: column;
  transition: transform 0.22s ease; transform: translateX(0);
  box-shadow: -8px 0 24px rgba(0, 0, 0, 0.08);
}
#drawer.drawer-closed { transform: translateX(105%); box-shadow: none; }
#drawer-header {
  display: flex; align-items: center; justify-content: space-between;
  padding: 12px 16px; border-bottom: 1px solid var(--border, #e4ddd0); font-weight: 600;
}
#drawer-body { flex: 1; overflow-y: auto; padding: 0 16px 16px; }
#drawer-body .panel { display: none; }
#drawer-body .panel.active { display: block; }
@media (max-width: 900px) { #drawer { width: 100vw; } }
```

- [ ] **Step 7: 构建验证（chat.js 未改完前 app 还跑不通，仅验证打包）**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npm run build:desktop`
Expected: 构建 + smoke-desktop-load 通过（renderer 是运行时脚本，打包不校验引用）。

- [ ] **Step 8: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add desktop/renderer/ && git commit -m "feat: task-workbench IA skeleton — sidebar + drawer, crew-bar retired"
```

---

## Task 6: chat.js 会话流 + onboarding 适配 + 文案修正

**Files:**
- Modify: `desktop/renderer/chat.js`
- Modify: `desktop/renderer/onboarding.js`

- [ ] **Step 1: chat.js 改造**

1. 头注释改：「会话连续性由 conversation_id 承担（S2.8 主进程持久化），renderer 不再持有 history」。
2. `const chatHistory = [];` 替换为 `let activeConversationId = null;`
3. `appendChatMessage` 与 `appendCardToStream` 函数体第一行各加 `exitHeroMode();`（任何流内容出现即退出 hero 空态——onboarding 卡片、审核员卡同样受益）。
4. 新增三个函数（放在 `appendChatCards` 之后）：

```js
function exitHeroMode() {
  document.getElementById("main-area").classList.remove("hero-mode");
}

/** 新任务：清流、回 hero 空态。首条消息发出才建会话（零仪式感）。 */
function newTask() {
  activeConversationId = null;
  document.getElementById("chat-stream").innerHTML = "";
  document.getElementById("main-area").classList.add("hero-mode");
  if (typeof refreshConversationList === "function") refreshConversationList();
  renderHeroChips();
  document.getElementById("chat-input").focus();
}

/** 任务历史回放：文字 + 卡片按发送时顺序重渲染（卡片在回复文字前，与实时一致） */
async function loadConversation(id) {
  if (chatBusy) { showToast("正在干活，稍等片刻再切换任务"); return; }
  const res = await safeInvoke(window.autocrew.conversationsGet, { id });
  if (!res.ok) {
    showToast(res.error || "无法打开该任务");
    if (typeof refreshConversationList === "function") refreshConversationList();
    return;
  }
  activeConversationId = id;
  document.getElementById("chat-stream").innerHTML = "";
  exitHeroMode();
  const messages = (res.data && res.data.messages) || [];
  for (const m of messages) {
    if (m.role === "assistant") {
      appendChatCards(m.cards);
      appendChatMessage("assistant", m.content);
    } else {
      appendChatMessage("user", m.content);
    }
  }
  if (typeof refreshConversationList === "function") refreshConversationList();
}
```

5. `sendChat` 内 invoke 改为：

```js
  const payload = { message };
  if (activeConversationId) payload.conversation_id = activeConversationId;
  const res = await safeInvoke(window.autocrew.chatTurn, payload);
```

6. 成功分支（`chatHistory.push` 两行删除）改为：

```js
  if (res.data.conversationId) activeConversationId = res.data.conversationId;
  appendChatCards(res.data.cards);
  appendChatMessage("assistant", res.data.reply);
  refreshActiveDrawer();
  if (typeof refreshConversationList === "function") refreshConversationList();
```

7. needsSetup 分支：`switchPanel("settings")` → `openDrawer("settings")`；文案「打开右侧「设置」」→「打开侧边栏「设置」」。
8. 删除 `bootChatWelcome` 整个函数（hero 空态即欢迎）。
9. 文件末尾新增 hero chips（字段名以 scout.js / settings.js 实际读取为准——执行者先核对 radar:status / knowledge:status 返回结构，红灯纪律允许最小修正）：

```js
/** Hero 上下文 chip：赛道 + 知识库状态（只读，点击开对应抽屉） */
async function renderHeroChips() {
  const wrap = document.getElementById("hero-chips");
  if (!wrap) return;
  wrap.innerHTML = "";
  const radar = await safeInvoke(window.autocrew.radarStatus);
  const industry = radar.ok && radar.data && radar.data.industry ? radar.data.industry : null;
  const indChip = h("button", { class: "hero-chip", title: "点击打开侦察员面板调整定位" },
    "赛道：" + (industry || "未设置"));
  indChip.addEventListener("click", () => openDrawer("scout"));
  wrap.appendChild(indChip);

  const kb = await safeInvoke(window.autocrew.knowledgeStatus);
  const kbReady = kb.ok && kb.data && (kb.data.entries || kb.data.documents || kb.data.count);
  const kbChip = h("button", { class: "hero-chip", title: "知识库详情在设置中查看" },
    "知识库：" + (kbReady ? "已就绪" : "未导入"));
  kbChip.addEventListener("click", () => openDrawer("settings"));
  wrap.appendChild(kbChip);
}
```

- [ ] **Step 2: onboarding.js 适配**

1. `bootOnboarding` 已完成分支：`bootChatWelcome();` → `return;` 前直接删调用（保持 hero 空态即欢迎）：

```js
  if (!res.ok || (res.data && res.data.onboarded)) {
    return; // 老用户：hero 空态即欢迎（S2.8）
  }
```

2. `finishOnboarding` 内 `refreshActivePanel();` → `refreshActiveDrawer();`；文案「右侧「风格」可改可停用」→「侧边栏数字员工 → 编剧 可改可停用」。

- [ ] **Step 3: 全量引用核查**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && grep -rn "switchPanel\|refreshActivePanel\|bootChatWelcome\|chatHistory" desktop/renderer/`
Expected: 零命中。

- [ ] **Step 4: 构建**

Run: `cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npm run check && npm run build:desktop`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add desktop/renderer/chat.js desktop/renderer/onboarding.js && git commit -m "feat: conversation-driven chat — id-based turns, history replay, hero empty state"
```

---

## Task 7: CDP 真机冒烟（含重启持久化全链路）

**Files:** 无代码改动；产出截图证据 `/tmp/autocrew-s28-smoke/*.png`

冒烟纪律（持久记忆）：隔离 HOME + `--use-mock-keychain` + 独占端口 **9444**（9333 被 chrome-devtools MCP 占用；9555/9666/9777/9888 已用过）+ 用完 pkill；连上 CDP 第一步验证 page.url 指向 worktree 构建。

- [ ] **Step 1: 预播种会话 fixture（绕过真实 LLM key）**

```bash
SMOKE_HOME=/tmp/autocrew-s28-home && rm -rf "$SMOKE_HOME" && mkdir -p "$SMOKE_HOME/.autocrew/conversations/conv-1700000000000-fixture" && cat > "$SMOKE_HOME/.autocrew/conversations/conv-1700000000000-fixture/meta.json" <<'EOF'
{ "id": "conv-1700000000000-fixture", "title": "Excel 快捷键口播（fixture）", "createdAt": "2026-06-11T08:00:00.000Z", "updatedAt": "2026-06-11T08:00:00.000Z", "turns": 1 }
EOF
cat > "$SMOKE_HOME/.autocrew/conversations/conv-1700000000000-fixture/messages.json" <<'EOF'
[
  { "role": "user", "content": "帮我写一条 Excel 快捷键的抖音口播", "ts": "2026-06-11T08:00:00.000Z" },
  { "role": "assistant", "content": "稿子在卡片里，看看节奏合不合适。", "ts": "2026-06-11T08:00:00.000Z",
    "cards": [{ "type": "draft", "data": { "contentId": "content-1-fixture", "title": "3 个 Excel 快捷键", "body": "今天教你三个快捷键……", "platform": "douyin", "hashtags": ["#excel"], "violations": [] } }] }
]
EOF
```

- [ ] **Step 2: 构建并启动**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && npm run build:desktop && HOME=/tmp/autocrew-s28-home npx electron desktop/dist/main.cjs --remote-debugging-port=9444 --use-mock-keychain &
```

- [ ] **Step 3: CDP 断言清单（chrome-devtools MCP 连 http://localhost:9444；先 `page.url` 含 `.worktrees/sprint2.8`）**

1. **Hero 空态**：`#main-area.hero-mode` 存在、`#hero-block` 可见、两枚 chip 渲染（「赛道：…」「知识库：…」）；截图 `01-hero.png`。
2. **侧边栏**：任务历史含「Excel 快捷键口播（fixture）」；数字员工 4 项；「定时任务/插件/消息渠道/团队」带 disabled 类；底部「设置」「本地版」。截图 `02-sidebar.png`。
3. **回放**：点击 fixture 任务 → stream 出现 user 气泡 + draft 卡（标题「3 个 Excel 快捷键」）+ assistant 气泡，顺序正确；hero-mode 类已移除。截图 `03-replay.png`。
4. **needsSetup 不落盘**：输入「测试」发送 → 出现引擎未配置引导气泡 + 「打开设置」按钮；点击按钮 → 设置抽屉滑出。`ls $SMOKE_HOME/.autocrew/conversations | wc -l` 仍为 1。截图 `04-needs-setup-drawer.png`。
5. **抽屉行为**：Esc 关闭；数字员工→编剧 → 风格抽屉开；数字员工→合规审核员 → 流内出现审核员说明卡；卡片「在工作区打开」→ 稿件抽屉开（S2.7 工作台回归：fixture content 不存在时列表为空态即可，不应报错崩溃）。截图 `05-drawer.png`。
6. **删除**：fixture 任务行 hover → ✕ → confirm 接受 → 列表回「还没有任务」、中央回 hero 空态；`ls` 计数 0。截图 `06-delete.png`。
7. **重启持久化**（核心验收）：重新播种 Step 1 fixture → kill electron → 重启同参数 → 任务历史仍列出 fixture、点击回放卡片俱在。截图 `07-restart-replay.png`。

- [ ] **Step 4: 清理**

```bash
pkill -f "remote-debugging-port=9444"; rm -rf /tmp/autocrew-s28-home
```

- [ ] **Step 5: Commit（如冒烟期间有修复）**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add -A && git commit -m "fix: smoke findings — <描述>"
```

---

## Task 8: 文档收尾

**Files:**
- Modify: `docs/dogfood-runbook.md`（新增「任务工作台」一节：新任务/任务历史/删除/数字员工抽屉/hero chip；全文 grep「右侧」「工作区」「28」等旧 IA 表述同步更新）
- Modify: `docs/PRD-v3.md` §7.3（交互模型增补：对话持久化已落地、IA 形态更新，标注 S2.8）
- Modify: `docs/superpowers/plans/2026-06-11-v1-capability-roadmap.md`（S2.8 标记完成 + 一行实况注记）

- [ ] **Step 1: 三份文档更新**（执行者按现有文风增补，引用本计划与 spec 路径）
- [ ] **Step 2: Commit**

```bash
cd /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git add docs/ && git commit -m "docs: task workbench runbook + PRD §7.3 + roadmap S2.8 done"
```

---

## 合并收尾（固定序列，主检出执行）

```bash
cd /Users/macmini/projects/AutoCrew && git merge feature/sprint2.8-task-workbench && npm run check && git worktree remove /Users/macmini/projects/AutoCrew/.worktrees/sprint2.8 && git branch -d feature/sprint2.8-task-workbench
```

推送前向创始人汇报冒烟截图证据。

---

## Plan 自审记录

- **Spec 覆盖**：§2 数据流→Task 3/4；§3 存储→Task 1/2；§4 IPC→Task 4；§5 IA（侧边栏/hero/抽屉/crew-bar 退役/onboarding 平移）→Task 5/6；§6 错误处理→Task 1/2（容错）+ Task 6（toast/确认）+ Task 7（验证）；§7 测试→Task 1-4 单测 + Task 7 冒烟；§8 YAGNI 边界未越。
- **类型一致性**：`ConversationMeta/ConversationMessage/Conversation` 全程一致；`runPersistedChatTurn` 的 `runTurn?: typeof runChatTurn` 与 chat-router 导出签名一致；`conversationsGet` 返回 `{meta, messages}` 与 chat.js 读取 `res.data.messages` 一致。
- **已知执行期校点**（红灯纪律内）：① hero chip 的 radar/knowledge 字段名以实际返回为准；② style.css token 名以 :root 为准；③ ipc.test.ts 第 147 行排除链的描述字符串同步更新。
